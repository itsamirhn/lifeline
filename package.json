{
	"name": "lifeline",
	"version": "1.0.0",
	"description": "A personal timeline feed application using Cloudflare Workers",
	"main": "src/index.js",
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev"
	},
	"dependencies": {
		"uuid": "^9.0.1"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"vitest": "~3.2.0",
		"wrangler": "^3.0.0"
	}
}